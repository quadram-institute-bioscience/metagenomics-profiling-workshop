---
title: Removing host reads
---

![Host Removal]({{ site.baseurl}}/{% link img/hostremoval.png %})

When extracting DNA from samples, if we want to focus on the microbiome we need to get 
a sample that has as limited contamination from unwanted organisms.

A typical example is the analysis of the gut microbiome: cells from the human host are usually
found in the samples (but typically negligible), while for other body sites (e.g skin) the amount
of human cells might be vastly superior to the microbiota cells.

In our workshop we won't focus on how to remove host cells from the samples (but methods [exists](https://www.nature.com/articles/s41592-023-02025-4),
and might even introduce further [biases](https://pmc.ncbi.nlm.nih.gov/articles/PMC10917613/))

## Removing host reads

There are different approaches, often based on mapping against a reference genome (i.e. the reads mapping are removed).
In general for processes like this we should consider:

1. Some regions are conserved across genomes, we might remove reads that are actually part of the microbiome (e.g. integrated retroviruses)
2. Sensitivity and specificity are traded-off
3. Choosing the right reference and eventually masking (removing) low-complexity regions or regions similar to bacterial 

So why dont' we simply leave the host reads?

1. The amount of host reads varies across samples, and this would make analyses more complicated trying to compare abundances
2. Several methods might classify  host reads as bacterial, inducing [to major errors](https://journals.asm.org/doi/10.1128/mbio.01607-23) especially in low-biomass samples

There is a host that is special: *Homo sapiens*. In our case we usually want to be more stringent in removing host reads (even at the risk of removing microbiome reads), to avoid patients data being identifiable. The genome is the most private biological property of an organism after all.


## Tools for host removal

[Hostile]() and [Deacon]() are modern and robust tools for host removal. The first is based on alignment against the reference
while the second uses a *k*-mer based approach, and is faster and requires less memory. We will use Deacon in this workshop.

## Using Deacon



Deacon is a host-read removal pipeline that uses its own aligner and index format to filter out contaminant reads.

:bulb: Deacon requires building a custom index from your host reference genome before filtering.

### Downloading coffee

You can use NCBI's `datasets` tool to download the `GCA_036785865.1` reference 
[as described here]({{ site.baseurl }}/{% link modules/Extras/_posts/2001-01-01-ncbi-datasets.md %}).

### Index reference (Deacon)

Note that the FASTA file can be masked to remove regions similar to bacterial segments, but we will skip this step
for the workshop.

Deacon uses its own indexing system. You will need to build an index with this syntax:

```bash
deacon index build --threads 8 --output coffee-index GCA_036785865.1.fna
```

* `GCA_036785865.1.fna` is the path to the FASTA file
* `coffee-index` is the basename for the output index files. You will use this when cleaning the reads

### Clean reads

The *deacon filter* workflow requires the FASTQ files (one if single-end, two if paired-end),
the index you built, and output paths for the cleaned files. When using `--deplete`, Deacon removes reads that align to the host reference.

The `--summary` flag produces a JSON file with statistics about host reads removed.

```bash
deacon filter --deplete --threads 8 \
  --output clean_1.fastq.gz \
  --output2 clean_2.fastq.gz \
  --summary stats.json \
  coffee-index \
  sample_1.fastq.gz \
  sample_2.fastq.gz
```

Replace the filenames with the paths to your actual reads and index.

### Summary statistics

The output JSON file will contain statistics about the amount of host reads removed.

```json
{
  "version": "deacon 0.15.0",
  "index": "giba/ref/deacon_coffea.index",
  "input": "./02_fastp/ERR2231570_1.fastq.gz",
  "input2": "./02_fastp/ERR2231570_2.fastq.gz",
  "output": "./03_deacon/ERR2231570_1.fastq.gz",
  "output2": "./03_deacon/ERR2231570_2.fastq.gz",
  "k": 31,
  "w": 15,
  "abs_threshold": 2,
  "rel_threshold": 0.01,
  "prefix_length": 0,
  "deplete": true,
  "rename": false,
  "rename_random": false,
  "seqs_in": 16178366,
  "seqs_out": 14382398,
  "seqs_out_proportion": 0.8889895308339544,
  "seqs_removed": 1795968,
  "seqs_removed_proportion": 0.11101046916604557,
  "bp_in": 4583354255,
  "bp_out": 4074027966,
  "bp_out_proportion": 0.8888747714745432,
  "bp_removed": 509326289,
  "bp_removed_proportion": 0.11112522852545684,
  "time": 38.855538445,
  "seqs_per_second": 438055,
  "bp_per_second": 124101609,
  "seqs_per_second_total": 416372,
  "bp_per_second_total": 117958840
}
```
 